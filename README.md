ZendService\LogEntries\Api component
================================

Provides support for LogEntries api

Install dependencies using:

```
curl -s https://getcomposer.org/installer | php
php composer.phar install
```
